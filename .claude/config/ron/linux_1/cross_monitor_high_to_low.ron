(
    // Wayland cross-monitor: saved on Monitor 0 (scale 1), launched from Monitor 1 (scale 2)
    // Tests size restoration when crossing from high DPI to low DPI
    position: None,
    width: 1000,
    height: 700,
    monitor_index: 0,
    mode: Windowed,
    app_name: "restore_window",
)

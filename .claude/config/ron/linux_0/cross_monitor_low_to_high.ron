(
    // Wayland cross-monitor: saved on Monitor 1 (scale 2), launched from Monitor 0 (scale 1)
    // Tests size restoration when crossing from low DPI to high DPI
    position: None,
    width: 1000,
    height: 700,
    monitor_index: 1,
    mode: Windowed,
    app_name: "restore_window",
)
